services:
  - type: web
    name: vault-mcp
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python server.py
    envVars:
      - key: GITHUB_TOKEN
        sync: false
      - key: VAULT_REPOS
        sync: false
